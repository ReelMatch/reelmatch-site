In ~/ReelMatch/reelmatch-site/editingroom/index.html, fix the Movies section. Currently it searches for movies by title query. Change the default behavior so that when the Movies section loads with an empty search box, it calls GET /movies/trending (with Bearer token) to show currently trending movies instead of doing a text search. Only switch to text search when the user actually types something and clicks Search. Show "Trending Now" as the section subtitle when displaying trending results, and "Search Results" when showing search results.